name: presentation_runtime
description: Riverpod presentation state, preparation, scheduling, and cache runtime for LicoUp.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  presentation_contract:
    path: ../presentation_contract
  riverpod: ^3.4.3

dev_dependencies:
  test: ^1.25.0
