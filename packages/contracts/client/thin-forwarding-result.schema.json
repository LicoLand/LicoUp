{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "packages/contracts/client/thin-forwarding-result.schema.json",
  "title": "ThinForwardingResult",
  "type": "object",
  "description": "Thin forwarding rule: model alias, upstream endpoint, auth token reference, request/response transformation hints.",
  "properties": {
    "rule_id": {
      "type": "string",
      "description": "Unique identifier for the forwarding rule."
    },
    "upstream_endpoint": {
      "type": "string",
      "format": "uri",
      "description": "URI of the upstream endpoint to which requests are forwarded."
    }
  },
  "required": ["rule_id", "upstream_endpoint"]
}
