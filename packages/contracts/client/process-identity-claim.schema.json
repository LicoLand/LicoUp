{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "packages/contracts/client/process-identity-claim.schema.json",
  "title": "ProcessIdentityClaim",
  "type": "object",
  "description": "Process identity claim token: public key fingerprint, capability set, expiry, signature.",
  "properties": {
    "fingerprint": {
      "type": "string",
      "description": "Public key fingerprint uniquely identifying the process."
    },
    "expiry": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp after which the identity claim is no longer valid."
    }
  },
  "required": ["fingerprint", "expiry"]
}
