{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "packages/contracts/client/local-runtime-status.schema.json",
  "title": "LocalRuntimeStatus",
  "type": "object",
  "description": "Local runtime status payload: process health, port binding, uptime, claim token validity, active features.",
  "properties": {
    "process_state": {
      "type": "string",
      "enum": ["running", "stopped", "crashed", "unknown"],
      "description": "Current health state of the local runtime process."
    },
    "uptime_seconds": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of seconds the runtime has been running."
    }
  },
  "required": ["process_state", "uptime_seconds"]
}
