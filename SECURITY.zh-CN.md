# 安全

[English](SECURITY.md) · 简体中文 · [首页](README.zh-CN.md)

## 报告问题

如果仓库提供私密漏洞报告功能，请优先使用。否则，请先通过私密渠道联系维护者，再发送
问题细节。不要在公开问题中发布可直接使用的攻击方法或私密数据。

只提供复现问题所需的最小示例。请使用虚构数据和占位符，例如 `<repo-root>`、
`<user-home>`、`<input-file>` 和 `<peer-id>`。

## 严禁公开

- 凭据、令牌、私钥、恢复材料或加密后的秘密。
- 用户消息、文件、智能体历史或原始工具输入与输出。
- 本地路径、账户数据、设备信息、日志或运行时报告。
- 受保护的服务数据或真实系统的原始命令输出。

## 客户端隐私边界

LicoUp 把敏感运行时数据留在设备上。对端传输由发送客户端加密，并发送给另一个
LicoUp 客户端。中转端不可信，LicoUp 不会向它发送用户内容明文。

用户批准的外部 MCP 请求属于另一条边界：HTTPS 会保护传输，但指定服务可以读取用户
批准的准确正文或文件。该请求不能继承对本机运行数据或未列明文件的访问权限。

手动配置的 OpenClaw 或 Hermes 虚拟机同样属于外部明文接收方。LicoUp 会持续显示
准确 SSH 目标；用户直接点击“发送”时，只授权把该条提示发给该虚拟机。系统 OpenSSH
使用 LicoUp 外部已经配置的非交互凭据完成认证并加密传输，同时强制主机校验。目标模型
不接受密码或私钥，关闭转发和连接复用，也不会把本机 MCP 服务描述转发到虚拟机。

插件安装、启用、客户端启动、定时任务和智能体请求都不能授权对外传输。调用方提供的
确认参数或可写状态文件不能证明用户已经批准。原生客户端命令会针对规范传输摘要请求
一次新的平台用户在场确认，并在交换前原子消费匹配的短期预览，且只能消费一次。摘要会
绑定方向、目标、用途、协议版本、会话和准确请求正文。每个准确请求和每个将要离开设备的
选中本地文件都必须取得新的准确批准；平台无法提供用户在场保护时，对外传输保持禁用。

可选本地运行器使用另一条独立信任边界。签名公钥必须与软件包下载分开导入，并作为平台
保护的权威状态保存。每次启动都重新校验不可变来源提交、完整签名清单、固定运行器、组装
载荷和进程身份。普通客户端状态只能作为投影，不能建立信任。

完整数据边界请阅读[架构说明](docs/architecture/README.zh-CN.md)。

## 协议权威与端点保管

Lico Arc Protocol 拥有稳定、线上可观测的 Pairwise Protection、Generic Message、
Reliable Exchange、协商与 Transport Profile 语义。LicoUp 执行一条固定 Protocol
Line，同时保留私钥、本地 Provider 配置、明文、会话历史、备份、用户信任、审批和
本地效果。通讯站不获得其中任何权威。

[当前正在退役的端点保护预览](docs/STATUS.zh-CN.md)不是 Lico Arc Profile 或
稳定兼容面。它不承诺未来互操作；完整固定 Lico Arc Protocol Line 替换它时会
直接退役。已经实现的 `licoarc.relay.v1` 候选外层 adapter 是另一项当前事实；
它的存在不会证明该预览符合 Lico Arc。

## 中转端威胁边界

不可信中转端可以观察路由字段和密文，也可以复制、丢弃、延迟、乱序或重放数据包。
LicoUp 不依赖中转端是否保存数据的承诺。发送端在网络传输前加密，接收端先检查对端
身份、数据完整性和重放状态，再使用内容。客户端不会向中转端发送用户内容明文或私钥。

所有运输服务默认都是恶意环境。LicoUp 与对端端点配对，不与运输服务配对。
运输配置和投递请求不会建立对通讯站的信任。BadTower 是独立 Lico Arc 通讯站，
LicoUp 的客户端直接适配器使用固定的 `licoarc.relay.v1` 候选协议，并已通过实际
BadTower 候选进程完成一次有界的双全新端点交换。这份本地候选证据不建立通讯站信任，
也不代表已发布、稳定支持或已托管运营。

LicoUp 不接受中转端下发的加密算法、密钥、信任根或安全策略。中转端提供的投递回执、
租约、时钟、过期报告和队列状态都是攻击者可控的运输提示，不能证明真实性、完整性、
新鲜性、防重放或对端最终接收。端点持有的加密状态和经过对端认证的协议证据才是这些
判断的权威。

## 当前本机密钥保管

客户端使用当前可用的平台安全存储，或者明确使用内存临时存储。使用内存临时保管时，
重启后需要重新配对并创建新密钥。当前存储接口可以保护静态的密封密钥数据，但不能证明
所有协议密钥都由硬件保护或不可导出。

当前平台适配器支持受保护访问时，LicoUp 会请求系统身份验证。取消或超时必须关闭失败。
客户端不接受中转端或服务端提供的可执行加密补丁，也没有运行时加密补丁加载器。
