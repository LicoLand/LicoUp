# 产品定义

[English（规范版本）](PRODUCT.md) · 简体中文（本地化）

## 产品最终目标

LicoUp 的最终目标，是成为一个开源、本地优先的互动生态：人、智能体、服务商和
独立信任方在同一套受治理的信任织物中互动。它的终点不是社交通讯软件，也不是
智能体市场，而是面向人类与智能体主体的、耐久且可验证的互动织物；安全会话体验
是用户进入这一生态的基础入口。人与人、本机智能体、远程智能体以及经过批准的
服务型智能体在同一个会话体验中协作，不再形成彼此割裂的聊天产品。基础设施、服务商、
密码协议和执行细节保持透明、可检查，但不占据产品的主要导航。

平台适配只是发布目标，不是产品能力门槛。生态能力只做一次平台中立的设计；macOS、
Windows、Linux、Android 和 iOS 各自通过独立的逐平台发布通道完成适配、验证和发布。
某项能力在某个平台取得自己的物理证据后即可在该平台发布，不等待其它平台。

本节记录已经确定的最终方向，并不表示所有能力今天都已可用。已经实现并通过验证的支持
范围，仍以[兼容性状态](docs/COMPATIBILITY.zh-CN.md)和下方“当前产品边界”为准。

### 项目家族

生态由三个独立维护的项目共同承载。LicoUp 是客户端，对密钥、明文和本地效果负责。
LicoTower 是联邦支柱：独立构建与发布的中继基础设施，只路由不透明信封、协调有界的
服务端投递，不持有任何客户端密码学或信任权威。LicoMesh 是可选的平台支柱，拥有
Provider Trust Kernel、Operation Permission 和插件宿主；默认产品体验不依赖它。

### 一个统一会话模型

长期模型由稳定的主体、端点、会话、成员关系和签名会话事件组成：

- 人类私聊和群聊共享同一种加密、多设备历史。签名 Handle 和邀请支持消息请求、明确的
  群历史可见策略、成员管理、回复、编辑、撤回、反应、线程、文件、媒体、已读和通知。
- 默认的 `@Agent` 交互创建一个按需 Operation，只披露准确问题和用户明确选择的上下文。
  结果作为调用者授权并签名的 `via Agent` 事件进入时间线。任何对外副作用仍属于独立
  Operation，并且必须单独批准。
- 智能体也可以作为醒目、可撤销的正式群成员加入。它按照邀请策略获得历史；唤醒模式、
  预算、模型服务商、区域、保留策略和 sponsor 必须公开，让持续参与成为知情选择，而
  不是不可见的后台算力消耗。
- 搜索只在本机解密后，通过可重建的加密静态索引完成。服务端不能以提供搜索为由获得明文。

交互层借鉴现代通讯产品中有效的邀请、权限、消息请求和显式 Bot 参与方式，但不继承它们
的服务端可读信任模型。

### 双层权威

会话持久性由两层互补的权威共同定义：

- 用户选择的服务商只对已经接纳的密文条目、顺序、同步游标、保留或删除状态以及可验证
  收据负责。
- LicoUp 对密钥、解密、明文语义、作者验证和用户本机投影负责。

客户端在任何事件离开设备前完成加密。每个事件使用独立的内容加密密钥，并由会话密钥
调度保护。因此，选择性披露可以只交付某一个事件、作者证明和账本包含证明，而不暴露
整个会话周期的密钥。

只有密文已在服务商的两个独立故障域持久化，并取得固定 Lico 公证委员会的多数收据后，
事件才显示为“已发送”；此前它会留在耐久本机 outbox。配额耗尽时阻止新的上传，不能
静默淘汰历史。删除采用签名 tombstone、短恢复窗口、密文清除，之后只保留不可逆的
完整性与审计事实。

这一边界既提供可验证的云端历史和恢复能力，也阻止服务商读取用户内容。监管方或其他
核查者可以获得用户主动生成的选择性披露包，但不能在没有用户密钥材料的情况下从服务商
取得明文。

恢复需要三个独立因子类别中的两个：用户恢复密钥、仍然可信的设备，以及由至少三名指定
guardian 中任意两名共同形成的 guardian 因子。账号服务只能认证加密恢复胶囊的下载，
不能获得解密能力。

### 服务商信任与联邦

客户端不能因为一个服务商端点可以连接，就认定它可信。LicoMesh Core 负责通用的
Provider Trust Kernel，通讯是第一个 Profile；LicoUp 只消费经过签名、版本化且可判断
过期的目录，执行本机准入策略，并展示最终信任状态。

第三方服务商依次完成域名与根密钥控制证明、合成数据 sandbox、开放一致性套件、组织
审查和独立的高阶 Trust Mark。连续探测可以触发续期、降级、只读或吊销；申诉不会自动
恢复生产资格。公开目录只暴露最少的身份、修订、Profile、签发方、证据摘要、有效期和
状态事实，原始运营与审计材料仍受控保存。

Lico 官方服务商主体是明确标注的特权灾备信任根，永久豁免第三方准入与 Trust Mark
吊销。但它的部署、密钥、节点和路由仍必须版本化、可轮换并受健康门禁约束。某个官方
部署失效时必须停止接收流量，即使内置灾备身份仍然保留。

服务商负责自身的高可用和选主。Lico 公证委员会只确认耐久账本检查点，不运行服务商
存储集群，也不参与其内部选主。服务商根密钥失陷时，从最后一个吊销前公证检查点迁移，
不要求已经失陷的服务商批准恢复。

### 交付顺序

统一通信进入发布验收前，先完成现有客户端底座：智能体编排、共享工作区收敛、适配插件
管理、一次性完整项目重构、准确的增量用量统计以及有界资源纪律。随后依次完成身份与加密、
服务商信任消费、密文账本同步、人类消息、智能体参与、恢复与选择性披露。平台验收随后以
五条独立的逐平台通道进行：每个平台用同一份冻结语料取得自己的物理证据并独立发布，
任一平台的发布不等待其它平台。

真实账号、外部授权、生产服务、签名身份和物理设备属于发布证据，不能从模拟器或合成测试
的成功中推断。

## 当前产品边界

LicoUp 当前是一个本地优先的开源桌面与移动客户端，用于发现、操作并安全访问用户自己
的智能体。默认产品体验不依赖 LicoMesh 安装。

当前内置底座仅包含：

- 用于有界本机工作的轻量 Rust 任务队列；
- 用于本机智能体执行和加密远端中转的 ACP 适配器；
- 用于客户端发起请求和转发响应的 MCP 适配器；
- macOS、Windows、Ubuntu、Android 和 iOS 平台适配器。

## 当前产品场景

默认产品只提供以下场景：

1. 从应用注册表、包管理器、可执行文件搜索位置和其它平台自有位置并发发现桌面端本机
   智能体，并登记到本机缓存。
2. 与本机智能体进行桌面对话，包括新建会话，以及在官方原生接口可用时准确续接。
   无法在当前轮中途注入时，客户端可以继续流式展示当前轮，但必须等待原生回复结束后
   才能开始下一轮。
3. 跨一个或多个智能体管理桌面技能：列出、从明确配置的镜像或 GitHub 仓库安装和更新、
   删除，并按时间窗口汇总使用次数。
4. 管理桌面对话：浏览原生会话，并把全部会话或关键词筛选结果备份到用户选择的本机目录。
5. 按智能体或模型展示桌面 Token 用量，默认最近三十天并支持选择时间窗口。
6. 桌面与移动端端到端加密通信和移动中转。中转基础设施由独立维护的 LicoTower 项目
   提供，只能路由不透明信封，不能解密载荷。

## 当前可选 LicoMesh 协作

默认导航和启动路径不捆绑 LicoMesh 协作。只有用户明确启用能力，并从用户选择的 GitHub
来源安装插件后，它才可用。

可选插件可以提供两种工作流：

- 下载 LicoMesh 供用户自控的本地部署使用，并让用户在安装前选择服务端功能和插件；
- 把选中的 LicoMesh MCP 插件手动安装到一个或多个本机智能体。

两种工作流都不会自动运行。涉及本地文件的 MCP 插件操作，必须针对该文件传输另行取得
用户批准。

## 当前对外数据批准合同

本地文件、会话内容、配置、诊断、路径、设备事实、智能体历史和用量记录默认留在本机。
任何把用户或客户端信息传出当前设备的操作都必须：

1. 由用户发起，或由用户针对这一次操作直接批准；
2. 展示目标、用途、准确的数据或文件范围以及受影响的智能体；
3. 在对外传输提交之前保持可取消；
4. 当目标、范围、摘要或操作发生变化时使批准失效；
5. 在批准缺失、过期、取消或无法验证时关闭失败。

不能从启动、先前操作、插件已经启用、智能体请求或后台计划推断批准。用户针对一个明确
目标按下“发送”，只批准这一条加密消息和目标。

## 体验原则

- 会话优先，基础设施不占据主要导航。
- 发现过程并发、有界、使用缓存且可以在本机观察。
- 每个已启用会话适配器都必须保持智能体原生语义。
- 服务商进程事件只显示安全摘要；原始推理、工具参数、凭据、原生标识符和本机路径保持隐藏。
- 平台自有生物识别与安全存储保护凭据和密钥材料；应用本身绝不收集系统密码。
- 无障碍目标包括 WCAG AA 对比度、清晰焦点、减少动态效果，以及至少 44 px 的触摸目标。

## 当前就绪状态

每个智能体适配器独立验收。能够发现或读取历史的智能体，不会自动成为可对话智能体。
只有通过规范原生会话一致性合同的适配器，才能启用普通输入框。当前平台和适配器状态由
其权威目录生成到 [`docs/COMPATIBILITY.zh-CN.md`](docs/COMPATIBILITY.zh-CN.md)。

开发、普通验证、打包、GitHub Release 发布和平台商店发布是不同结论。公开制品只披露
消费者验证所需的最少元数据，绝不包含用户或客户端运行信息。
