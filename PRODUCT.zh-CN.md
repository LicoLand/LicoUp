# LicoUp 产品

| 关联文档 | 语言 / 路径 | 权威职责 |
|:---|:---|:---|
| **规范版本** | [English (Normative)](PRODUCT.md) | 产品定义与设计哲学英文规范 |
| **当前状态** | [docs/STATUS.zh-CN.md](docs/STATUS.zh-CN.md) | 当前实现状态与发布证据 |
| **兼容性矩阵** | [docs/COMPATIBILITY.zh-CN.md](docs/COMPATIBILITY.zh-CN.md) | 平台与 13 个智能体支持度 |
| **领域词汇** | [CONTEXT.md](CONTEXT.md) | 统一领域词汇与定义 |
| **架构总览** | [docs/architecture/README.zh-CN.md](docs/architecture/README.zh-CN.md) | 四层顶层客户端架构与总览 |
| **项目首页** | [README.zh-CN.md](README.zh-CN.md) | 仓库主页与导览 |

LicoUp 是一个开源、本地优先的人类—智能体会话客户端。

它的长期终局是一套安全会话体验：人与醒目可见的智能体在用户控制的身份、审批、披露和本地效果边界内共同参与。基础设施、服务商、密码学与执行细节保持可检查，但不占据主要导航。当前实现、验证、发布、支持与运营事实只由 [`docs/STATUS.zh-CN.md`](docs/STATUS.zh-CN.md) 和生成的 [`docs/COMPATIBILITY.zh-CN.md`](docs/COMPATIBILITY.zh-CN.md) 记录。仓库领域词汇以英文 [`CONTEXT.md`](CONTEXT.md) 为准。

## 设计理念

- **多元** — 通过适配器连接不同智能体和设备，不绑定单一厂商。
- **互联** — 让本机工具与对端客户端使用清晰透明的连接流程。
- **开放** — 源代码和客户端契约都可以检查和扩展。
- **融合** — 用统一应用与桥接契约隔离界面与具体适配器。

## 产品承诺

用户可以与自己的智能体、对端端点和明确准入的外部能力沟通，同时继续控制
受保护内容、密钥、审批与本地效果。

客户端只在当前用户操作真正需要某项系统隐私权限时才向操作系统申请。自动发现
通过用户配置的命令行环境解析每个 Agent 的规范命令，但不会为探测而启动 Agent。
这种有界查询可以通过 Shell 识别 PATH、命令别名、函数、wrapper 和 shim，但不会
递归枚举 PATH 目录。Agent 扫描路径清单只补充发现配置、历史、其他安装版本和
LicoUp 管理的运行时。发现过程不得集中弹出桌面、文稿、下载、图片、音乐、照片
图库、媒体资料库、网络宗卷、麦克风、摄像头或其他 App 数据的授权。Token 用量
在打开监测页之前不会扫描。

在人类消息、联邦、恢复和多设备目标交付前，产品仍可先作为本机智能体
客户端使用。这些长期目标只有在各自实现与验证闭环后，才成为当前能力。

## 持续协作的产品目标

用户应当可以一直与同一个可见的 Assistant 合作，无需管理服务商 Session、刷新
对话、选择编码或目标模式，或手动把资料搬进一个记忆界面。Assistant 理解当前
是在询问、探索、委托还是回到旧工作，并在需要时调动专业能力；软件开发只是
其中一类工作。

切换话题不应污染无关工作，也不应丢弃未完成的承诺。稍后返回时，应恢复相关
约定、证据与进展，而不要求用户重复背景。用户仍可自由新建、组织和分隔
Conversation；稳定的 Assistant 体验不会合并私人和共享会话的可见范围。

自动组织减少管理劳动，不减少用户控制。真实参与者、工作来源、资源消耗和
外部目标保持可检查。必要审批、用户纠正、显式 Assistant 指定与原生 Agent
能力继续属于协作体验。持续协作机制与采纳边界由已接受的
[持续 Assistant 架构](docs/architecture/CONTINUOUS-ASSISTANT.zh-CN.md) 拥有。

## Agent 命令身份

在用户没有通过智能体中心显式覆盖时，LicoUp 必须选择用户在其配置的命令行环境中
输入规范命令时会选中的同一个 Agent 命令。这个命令选择边界包括 Shell 启动语义、
PATH 顺序、wrapper、版本管理器 shim，以及 alias 或 function 带来的目标、参数与
环境。只有在保持行为不变时，LicoUp 才能把结果折叠成一个具体可执行文件；否则必须
保留由 Shell 承载的启动绑定，不能静默丢弃用户配置。

智能体中心展示观测到的命令绑定和所有额外候选。用户显式选择命令或版本后，该选择
取代观测到的默认值；用户配置的环境变量、参数与 Hook 再以可见且确定的优先级扩展
所选启动配置。当命令行环境中不存在对应命令时，可以提供扫描清单发现或 LicoUp
管理的运行时，但不得把它表述成用户的终端选择。

## 一套会话模型

长期模型让人与智能体共同出现在一套醒目的会话中：

- 用户可以新建、继续、搜索、整理和保存会话；
- 本机或远端智能体通过明确准入的接口参与，不会自动获得权威；
- 人或智能体只获得会话成员策略明确授予的历史与上下文；
- 每次对外披露或效果都保持可见、有界并单独批准；
- 本地搜索与投影只在端点控制的解密后建立。

服务商管理的云端历史可以在用户授权对应服务商后读入体验。它仍然是服务商拥有
的来源，不能静默成为 LicoUp 规范 Conversation 权威。

## 可信历史与恢复

产品方向包含一条由服务商管理的云端历史路径。取得服务商授权后，保留的历史默认
可读；默认历史读取不会调用恢复密钥。该历史的客户端加密必须显式选择，默认路径
不会静默启用。

历史恢复会还原获得授权的服务商仍然提供的全部保留对象。服务商访问规则仍然
有效；恢复不能绕过这些规则，也不能重新创建缺失、删除、过期或其他不可用的对象。
身份恢复材料会单独保护与验证，默认历史读取永远不依赖它。替换设备恢复会先准备
身份权威和全部仍可用的保留历史，再将两者原子发布；仅有身份材料无法重新创建缺失
的历史。

LicoUp 不会通过通讯站传送或在通讯站存储这些历史；恶意通讯站始终位于历史路径
之外。这条历史路径不要求或承诺公证服务或端点证明。LicoUp 展示的任何证据都保持
在本地，并且只对产生它的操作有效。

## 端点职责与协议执行

LicoUp 拥有：

- 端点身份材料、私钥保管，以及本地密码 Provider 的选择与调用；
- 解密后的明文、规范 Conversation 历史、经服务商授权的历史投影、本地搜索投影
  和用户选择的备份；
- 对一条固定 Lico Arc Protocol Line 的合规执行；
- 端点准入、用户对对端的信任决定、审批、受保护披露和本地效果；
- 对协议定义的新鲜性、防重放与恢复迁移进行本地持久化和执行；
- 客户端配置、原生桥接、平台适配、打包与用户体验。

端点保护不依赖任何通讯站或网关实现。通讯站响应、租约、时间、队列状态、
确认或送达声明都只是不可信的运营提示。

LicoUp 不拥有可以单方面分叉的另一套密码协议。线上可观测的 Pairwise
Protection、Generic Message、Reliable Exchange、协商、Transport Profile
与协议迁移语义都由版本化 Lico Arc Protocol Line 治理。LicoUp 选择受支持的
Protocol Line，持有端点密钥与本地状态并执行该线路；无法合规时关闭失败。

## 联邦边界

Lico Arc Protocol 位于 LicoUp 之外，是稳定、线上可观测端点通信的实现中立
权威。它拥有版本化 Pairwise Protection、Generic Message、Reliable Exchange、
协商、Transport Profile、通讯站侧契约、一致性语料和中立联邦治理，但不获得
私钥、本地 Provider 配置、明文、会话历史、备份、用户信任决定、审批权威或
本地效果权威。

BadTower 是独立版本化的单节点通讯站产品。它只在自己的生命周期中独立发布；
从端点视角看，它只存储和转发不透明 Lico Arc 信封，并始终可能是恶意环境。
它不是 LicoUp 后端，也不定义端点身份、密码学、审批、对端信任或用户体验。
兼容只能通过具名 Lico Arc Protocol Line 与独立一致性证据建立，不需要产品
专用协议、链接实现或同步发布。

## 信任分权

- Lico Arc Protocol 拥有线上可观测的 Pairwise Protection、Generic Message、
  Reliable Exchange、协商、Transport Profile、联邦治理与协议兼容；
- LicoUp 持有私钥、选择并调用本地 Provider、执行固定 Protocol Line，并拥有
  明文、规范历史、经服务商授权的历史投影、备份、端点准入、审批、本地效果与
  用户最终信任决定；
- 通讯站不拥有任何信任决定。

这些状态必须彼此独立，不能合并为跨产品的通用信任结论。

## 官方网络边界

LicoLand 运营的 LicoUp 网络只有取得独立发布与运营证据后，才可以成为可替换
便利默认入口。它没有密码学、身份、准入、认证、吊销、路由或灾备特权。没有
该网络时，客户端仍应可用。LicoLand Network Host 只拥有该运营者的集群部署
与运营证据；LicoUp 继续拥有客户端默认入口选择和所有端点信任决定。

## 外部操作边界

外部操作必须取得新的直接审批，并绑定准确目标、用途、范围和内容。安装、
启用、启动、定时任务、智能体意图或历史审批，都不能授权后续对外披露或效果。

传输保护不能阻止获批目标读取用户明确发送给它的准确内容。发往对端端点的
受保护传输与外部服务请求是不同概念。

Gateway Runtime 上的 Communication Channel 是获准接入的外部通道：启用运行时
并批准 Telegram 私聊配对后，即为该 bot 与该 Telegram 用户授予与已绑定本机
智能体交换普通回合的有界桥接授权。撤销配对即结束授权。经 Telegram 发送的
内容仍可被 Telegram 读取。

## 平台与发布模型

LicoUp 通过彼此独立的平台适配与发布通道面向桌面和移动平台。开发、源码验证、
平台构建、真机验证、打包、GitHub Release 与每个应用商店都是独立结论。一个
平台或渠道的证据不能提升另一个。

## 非目标

LicoUp 不会：

- 把端点安全或信任委托给通讯站、服务、插件或网站；
- 定义、稳定化或静默分叉产品专用端点线路协议；
- 把官方网络变成强制或有特权的信任根；
- 把服务商授权当作端点身份恢复，恢复已不可用的历史对象，或把通讯站变成历史存储；
- 把可构建、预览状态、生成产物或计划视为发布或支持证据；
- 把退役的产品专用通讯站线路保留为永久兼容面。

## 体验原则

- 会话优先，基础设施不占据主要导航；
- 本地优先，受保护客户端数据由端点控制；
- 每个已启用智能体 adapter 都必须保持原生保真；
- 对外目标和效果始终明确；
- 无障碍设计追求清晰焦点、减弱动态、强对比和触控友好尺寸。

## 许可证

LicoUp 使用 AGPL-3.0-or-later。详见 [`LICENSE`](LICENSE)。
